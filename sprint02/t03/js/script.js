'use strict'

function myFunc() {
  alert('Freedom is power');
}
myFunc();

function countMySquare(number) {
  return (number * number);
}

let n = 696;
alert(`The square of ${n} = ${countMySquare(n)}`);